<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="utf-8">
</head>
<body>
{{ range $index, $book := .book }}
<tr>
    <td>{{ $book.Name }} </td>
    <td>{{ $item.Owner}} </td>
    <td>{{ $item.Created_at }}</td>

</tr>
{{ end }}
</body>
</html>